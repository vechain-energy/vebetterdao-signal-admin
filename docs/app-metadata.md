# App Metadata

Signal and reset rows query both indexed app metadata and `metadataURI`.

When indexed metadata is missing a logo, the UI fetches the app `metadataURI` through the VeChain IPFS gateway and uses the loaded `logo`, `logoUrl`, `icon`, or `image` value.
